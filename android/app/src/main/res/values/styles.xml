 + <resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@android:color/transparent</item>
    </style>
    <style name="BootTheme" parent="Theme.BootSplash">
    <item name="android:windowBackground">@drawable/bootsplash</item>
    <item name="android:statusBarColor" tools:targetApi="m">@color/bootsplash_background</item>
    <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>v
    <item name="postBootSplashTheme">@style/AppTheme</item>
  </style>

</resources>
